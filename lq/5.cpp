#include <iostream>
using namespace std;

int main() {
    int n = 0;
    string S, T;
    cin >> n >> S >> T;
    for (auto &ele : S) {
        ele = ele <= 'z' && ele >= 'a' ? 
    }
    return 0;
}